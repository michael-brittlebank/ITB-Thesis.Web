(function(){

    var that = app.views.error404,
        $ = jQuery;

    this.init = function(){
        if ($('#page-error-404').length > 0) {
            
        }
    };

}).apply(app.views.error404);