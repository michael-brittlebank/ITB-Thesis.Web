(function(){

    var that = app.views.error500,
        $ = jQuery;
    
    this.init = function(){
        if($('#page-error-500').length > 0) {
           
        }
    };

}).apply(app.views.error500);