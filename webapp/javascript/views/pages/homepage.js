(function(){

    var that = app.views.homepage,
        $ = jQuery;

    this.init = function(){
        if($('#page-homepage').length > 0) {
            
        }
    };

}).apply(app.views.homepage);